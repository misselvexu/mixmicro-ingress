## mixmicro plugin config properties
mixmicro:
  ingress:
    scg:
      ## dynamic router config
      dynamic:
        router:
          enabled: true
          data-id: ingress-dynamic-router.json
    doc:
      enabled: true


## enabled sentinel
spring:
  cloud:
    sentinel:
      enabled: true
